<table class="tg">
  <% for (x = 0; x < 15; x ++) { %>
    <tr>
      <% for (y = 0; y < 15; y ++) { %>
        <td class="tg-031e">
          <div>
            <img id='img<%= x %>-<%= y %>' ng-src='/images/tiles/{{ scrbCtrl.tile(<%= x %>, <%= y %>) }}.png'>
          </div>
        </td>
      <% } %>
    </tr>
  <% } %>
</table>
